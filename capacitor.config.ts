import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'x-connectivity-and-crypto',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
